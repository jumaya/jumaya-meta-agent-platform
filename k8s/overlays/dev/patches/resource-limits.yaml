apiVersion: apps/v1
kind: Deployment
metadata:
  name: orchestrator
  namespace: meta-agent
spec:
  template:
    spec:
      containers:
        - name: orchestrator
          resources:
            requests:
              memory: "128Mi"
              cpu: "50m"
            limits:
              memory: "256Mi"
              cpu: "250m"
