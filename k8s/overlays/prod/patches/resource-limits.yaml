apiVersion: apps/v1
kind: Deployment
metadata:
  name: orchestrator
  namespace: meta-agent
spec:
  template:
    spec:
      containers:
        - name: orchestrator
          resources:
            requests:
              memory: "512Mi"
              cpu: "250m"
            limits:
              memory: "1Gi"
              cpu: "1000m"
